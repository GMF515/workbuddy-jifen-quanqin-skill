---
name: workbuddy积分全勤skill
description: "WorkBuddy 每日积分自动签到。读取本机登录态，调用官方接口领取当日签到积分（100 积分/天，连续第 7 天 1000 积分），今天领过会自动跳过。当用户说「签到」「领积分」「每日积分」「Buddy 加油站」「帮我设置自动签到」时触发。不适用于企业版账号（无签到活动）。"
version: "1.0.0"
license: MIT
---

# WorkBuddy 每日积分签到

帮用户领取 WorkBuddy 每日签到积分：100 积分/天，连续第 7 天 1000 积分，断签清空连续天数。

## 何时使用

- 用户说「签到」「领积分」「每日积分」「Buddy 加油站」
- 用户要求「设置每天自动签到」

## 何时不使用

- 企业版账号（没有签到活动）
- 用户问的是成长计划、派猫猫旅行、社区发文等其他积分入口（不在本技能范围）

## 执行方式

```bash
cd <skill目录> && node scripts/checkin.js            # 签到
cd <skill目录> && node scripts/checkin.js --dry-run  # 只查状态，不领取
```

首次使用或排查问题时，先跑 `--dry-run` 确认能读到登录态。

## 汇报要求

只汇报以下三种结果之一，不要展开细节：

| 情况 | 汇报 |
|---|---|
| 领到 | 签到成功，领取 X 积分，已连续 X 天 |
| 今天已领 | 今日已签到 |
| 失败 | 一句话说明原因（登录态失效 / 网络异常 / 接口异常） |

## 自动运行

用户想每天自动签：用 WorkBuddy 的定时任务即可（说「帮我设置每天自动签到」）。技能目录里的 bat 文件是可选的零积分方案（仅 Windows），不想要可以不用。

## 硬性约束

- 不输出、不记录登录凭证（token）的任何内容
- 只访问官方域名，不上传任何信息到第三方
- 只读本机登录态，不修改 WorkBuddy、不修改凭证文件
- 不安装任何额外依赖
- 网络异常只记一次失败，不重试，等下次触发

## 接口

- 查状态：`POST https://copilot.tencent.com/v2/billing/meter/checkin-status`
- 签到：`POST https://copilot.tencent.com/v2/billing/meter/daily-checkin`
- 鉴权：`Authorization: Bearer <token>`，另带 `X-User-Id`

## 返回码

| 码 | 含义 | 处理 |
|---|---|---|
| code=0 | 成功 | 报积分和连续天数 |
| code=10001 | 今天已领过 | 视为成功，报「今日已签到」 |
| 401 / 403 | 登录态失效 | 提示重新登录，不重试 |
| 其他 / 网络异常 | 异常 | 记一句话原因，不重试 |

注意：查状态里的「今天是否签到」字段不可靠，真正的防重复靠签到返回的 code=10001。

## 已知限制

- 仅限个人版账号
- 每月自动发的 500 分、成长计划、派猫猫等，本技能不覆盖
